version: "3.9"

services:
  docuchat:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data  # persist uploads and FAISS index
    # Uncomment for GPU (NVIDIA):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - capabilities: ["gpu"]
    # environment:
    #   - CT2_USE_CPU=0
    #   - CUDA_VISIBLE_DEVICES=0
